<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Kalkomey</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav w-100 mb-2 mb-lg-0">
                <% if user_signed_in? %>
                    <li class="nav-item col-md-1">
                        <%= link_to 'Hunts', '/hunts', :class => 'navbar-link' %>
                    </li>
                    <li class="nav-item col-md-1 offset-md-10">
                        <%= link_to "Logout", destroy_user_session_path, method: :get, :class => 'navbar-link'  %>
                    </li>
                <% else %>
                    <li class="nav-item col-md-1 offset-md-9">
                        <%= link_to 'Login', new_user_session_path, :class => 'navbar-link' %>
                    </li>
                    <li class="nav-item col-md-2">
                        <%= link_to 'Sign Up', new_user_registration_path, :class => 'navbar-link' %>
                    </li>
                <% end %>
                </ul>
            </div>
        </div>
    </nav>
</body>